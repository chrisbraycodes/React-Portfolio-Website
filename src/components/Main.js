import styled from 'styled-components';

// Styled container for centering and limiting content width
export const Container = styled.div`
    width: 90%; // Set container width to 90% of the viewport width
    max-width: 1200px; // Restrict the container's maximum width for readability
    margin: 0 auto; // Center the container horizontally
    padding: 2rem 0; // Add vertical padding for spacing
`;
